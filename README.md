## Installation

First, create a virtual Python environment (skip if you already have one):
```
python3 -m venv venv
```

Activate the environment and then install required packages:
```
pip install -e .
```

Run the flask server on the localhost to launch GUI
```
flask run
```
